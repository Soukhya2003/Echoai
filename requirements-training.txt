# ===== Full requirements including BERT training (NOT needed at runtime) =====
# Install locally only when retraining: py -3.11 -m pip install -r requirements-training.txt

-r requirements.txt

# BERT / Transformers (heavy, training-only)
transformers>=4.35.0
torch>=2.1.0
accelerate>=0.25.0
